import { StyleSheet } from "react-native";

export default StyleSheet.create({
  tabNavigatorStyles: {
    backgroundColor: "#1f1f1f",
    alignItems: "flex-end",
    padding: 0,
    height: 75
  }
});
